@model TripPlanner.Models.TripViewModel
@{
    ViewData["Title"] = "Add Things To Do";
    // if(Model != null)
    // {
    //     ViewData["SubHeading"] = "Add Things To Do in " + Model.Destination;
    // }
    if (ViewData["Location"] != null)
    {
        ViewData["SubHeading"] = "Add Things To Do in " + ViewData["Location"];
    }
    

}



<div class="wrapper form-page">
    <form asp-action="ThingsToDo" method="post">
        <div class="form-group">
            <label asp-for="ThingsToDo1">Thing to do</label>
            <input asp-for="ThingsToDo1" class="form-control" />
        </div>

        <div class="form-group">
            <label asp-for="ThingsToDo2">Thing to do</label>
            <input asp-for="ThingsToDo2" class="form-control" />
        </div>

        <div class="form-group">
            <label asp-for="ThingsToDo3">Thing to do</label>
            <input asp-for="ThingsToDo3" class="form-control" />
        </div>

        <div class="button-group">
            <button type="submit" class="btn btn-primary">Save</button>
            <a asp-action="Index" class="btn btn-secondary">Cancel</a>
        </div>
    </form>
</div>